# Documentation archive

Historical material retained for traceability.

- `releases/` — per-feature README snapshots from generated ZIPs;
- `reports/` — validation and manual QA snapshots;
- `feature-notes/` — superseded incremental implementation notes.

These files are **not** current implementation contracts. Prefer `docs/architecture/`, `docs/process/` and `docs/features/`.
