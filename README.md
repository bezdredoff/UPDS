# Project UPDS — «Детективы класса U»

Mobile-first visual novel × match-3 vertical slice.

Current build: **ANM-010 · GitHub Phone Pipeline** (`0.10.0-anm010`).

## Local

```bash
npm install
npm run dev
npm run check
```

## Phone-first GitHub workflow

See [`docs/GITHUB_PHONE_PIPELINE_RU.md`](docs/GITHUB_PHONE_PIPELINE_RU.md).

Protected project contracts: canon, stable VN line IDs, save key `seiran-detectives-anm009-v1`, approved art direction, and the `base + face overlay` production rig remain unchanged by ANM-010.
